export enum CampaignType {
  PayPerClick = 'pay_per_click',
  PayPerView = 'pay_per_view',
}

export enum AdPlacement {
  Homepage = 'homepage',
  TopOfSearchResult = 'top_of_search_result',
  SideBar = 'side_bar',
}
